language: java

sudo: required

services:
  - docker

jdk:
  - oraclejdk8

install:
  - sh ./.boot/database/oracle/install.sh

after_success:
  - mvn jacoco:report coveralls:report

script: mvn clean test

cache:
  directories:
    - $HOME/.m2
